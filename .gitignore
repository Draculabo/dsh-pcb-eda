node_modules/
dist/
# tsdown build outputs (keep source dirs named `lib`, e.g. */src/lib, tracked)
packages/*/lib/
coverage/
*.tgz
.DS_Store
*.log
# scratch DSH_HOME used by the stock-DSH smoke test (docs/tasks/start-p0.md)
.smoke/
