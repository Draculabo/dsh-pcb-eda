# DSH bundle patch: inserts the Huaqiu part-search tool plugin into a profile's layer stack.
- insert:
    - id: huaqiu-tool-part-search
      name: '@huaqiu/dsh-tool-part-search'
      inject: ['tools']
