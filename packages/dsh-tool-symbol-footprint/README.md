# @huaqiu/dsh-tool-symbol-footprint

Huaqiu symbol & footprint generation DSH tool plugin (datasheet image → symbol /
footprint, dimension-driven BGA/QFP footprint, componentV2 streaming).

- Dual-face package: node tools + browser HIT UI (React, Phase 3).
- Phase 0 = skeleton with a `huaqiu_phase0_probe` tool + client stub.
- Status: Phase 0 skeleton.
